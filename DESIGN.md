# Design System

## Product register

面向开发者和运维人员的 Java Swing 桌面产品界面。设计服务于高频任务，强调专业、克制和高效。

## Component library

共享组件位于 `com.aqishi.toolbox.ui.kit`，所有工具面板都基于它装配，不再各自手写布局。

| 组件 | 职责 |
|---|---|
| `Tokens` | 间距、圆角、控件尺寸、字体与语义颜色的唯一来源 |
| `Layouts` | 页面骨架：`page()`、`stack()`、`columns()`、`rows()`、`split*()`、`wrapRow()` |
| `Card` | 卡片容器：圆角描边 + 标题带 + 内容区 + 可选底部状态条 |
| `FormGrid` | 表单网格：标签列右对齐、输入列拉伸或紧凑 |
| `ActionBar` | 一行操作条：左侧说明、右侧按钮，窄窗口下不换行 |
| `Buttons` | 按钮语义：`primary` / `secondary` / `ghost` / `danger` / `snug` / `compact` / `toggle` |
| `Fields` | 输入控件工厂：统一高度、占位提示、滚动容器变体 |
| `KitBorders` | 卡片描边、带标题的卡片描边、单侧细线 |

## Layout

- 主窗口由一个分组侧边导航、一个内容工作区和底部状态栏组成。
- 侧边栏默认宽度 240px，最小 200px，最大 340px；底色与工作区略有区分，右侧一条细线分界。
- 侧边栏支持收起，并持久化宽度、收起状态、分组展开状态和当前工具。
- 当前工具栏始终位于内容区上方，以「分组 / 工具」面包屑呈现当前位置；状态信息始终位于窗口底部。
- 工具页统一使用 `Layouts.page()` 作为根容器，外边距只由它提供，内容区不再叠加第二层留白。
- 工具页按「配置 → 操作 → 结果」组织：配置区放 `NORTH`，结果区放 `CENTER` 吸收剩余空间。
  操作按钮归属于所在卡片的标题栏或 `ActionBar`，不单独占据页面中部。

## Spacing and density

- 间距刻度：4px、8px、12px、16px、24px。
- 导航行高 32px，表格行高 26px，卡片内边距 14px。
- 常规按钮、文本框和下拉框：统一 32px 高。
- 按钮的最小宽度等于首选宽度：布局紧张时宁可拥挤，也不把文字截断成省略号。

## Color and theme

- 颜色来自 FlatLaf 和 `UIManager`，通过 `Tokens` 推导，不硬编码浅色或深色壳层颜色。
- 卡片底色取内容类控件的背景（`TextField.background`），叠在工作区底色之上形成一层轻微高低差；
  因此文本域、表格放进铺满型卡片时不会出现双层边框或色差。
- 深浅主题由工作区底色亮度判定，不维护主题白名单。
- 选中、焦点、禁用、错误、警告和成功状态遵循当前主题，且不只靠颜色表达——状态文字必须同时说明。
- 全局外观默认值（圆角、控件高度、滚动条、表格、标签页）在 `ThemeManager.applyCustomDefaults()` 统一设置，
  且必须在 `FlatLaf.setup()` 之后写入，否则会被重建的 defaults 表覆盖。

## Typography

- 使用现有平台字体和 FlatLaf 标签字体，字号与字重由 `Tokens` 提供。
- 应用名称、分类、当前工具、卡片标题通过字重和颜色建立层级。
- 说明文字使用 12px 弱化色；代码、密钥、日志使用等宽字体。
- 水平空间不足时截断工具名称，并用 tooltip 提供完整文本。

## Interaction

- `Ctrl+K` 聚焦搜索；侧边栏收起时会先展开。
- 方向键移动工具选择，选中项与右侧内容保持同步。
- `Enter` 打开当前工具或展开分类，`Esc` 清空搜索。
- 搜索只过滤导航，不销毁已经打开的工具视图。
- 分类单击展开或收起并显示工具数量，侧边栏可拖动调整宽度。

## Accessibility

- 键盘焦点必须可见，选中状态不能只靠颜色表达。
- 搜索和导航提供本地化的可访问名称。
- 简体中文与 English 切换不会改变工具稳定 ID 或清空当前工具状态。
- 820×520 最小窗口下，全局导航、主题、语言和内容仍可到达，控件不被裁切。
