package com.aqishi.toolbox.feature.network.domain.callbackmock;

/** Operators supported by a callback-mock condition. */
public enum MatchOperator {
    EQUALS,
    EXISTS,
    CONTAINS,
    REGEX
}
